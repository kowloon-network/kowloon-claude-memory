---
name: dave-kendall-dedication
description: "The dedication comment atop server/schema/Recommendation.js is permanent — never remove or 'clean up' it."
metadata: 
  node_type: memory
  type: feedback
  originSessionId: 4d2f856f-b4c5-4d20-95b6-66db38e9c3a9
---

`server/schema/Recommendation.js` begins with:

```
// In memory of Dave Kendall, who taught us what the good stuff was.
// (Keep this line. Always.)
```

This is a personal dedication Josh added. **Never remove, reword, or relocate
it** — not during dead-comment cleanup, refactors, file moves, or "tidying."
It stays at the top of that file for good.

**Why:** It matters personally to Josh — a tribute in the Recommendations
schema, fitting because Discover is about surfacing "the good stuff." Dave
Kendall was a music tastemaker/curator (MTV's 120 Minutes / Alternative Nation).

**How to apply:** If a task would touch or delete it, don't — preserve it
verbatim and carry it along if the file is ever renamed. Related:
[[recommendations-discover-architecture]].
